<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme -->
    <style name="Theme.Mories.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Mories</item>
    </style>

    <!-- Base App Theme -->
    <style name="Theme.Mories" parent="android:Theme.NoTitleBar">
        <!-- Handled at runtime in MainActivity and Compose Theme -->
    </style>
</resources>
